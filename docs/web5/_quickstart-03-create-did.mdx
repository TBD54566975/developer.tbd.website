## 2. Create Web5 instance:

There’s only one class for all Web5 operations

```js
const web5 = new Web5
```

## 3. Create a Decentralized Identifier (DID):

In the world of Web5, your unique identifier, like an email address, is called a DID.

:::info
A [Decentralized Identifier (DID)](https://www.w3.org/TR/did-core/),
is an W3C specification for an address representing who you are on the decentralized web. You 
can [learn more about them here](/docs/web5/learn/decentralized-identifiers) as 
a core concept behind Web5.
:::

We can create a DID using the Web5 API:

```js
let did = await web5.did.create('ion');
```

This creates a new DID using the `ion` [method name](/docs/glossary#did-method---ion).

Play with creating DIDs here: