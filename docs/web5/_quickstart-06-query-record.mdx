## 6. Query

Here’s how you’re able to query anything that was saved in your DWN.

```js
let queryResult = await
    web5.dwn.records.query(did.id, {
        author: did.id,
        message: {
            filter: {
            schema: 'test',
        },
    },
});
```

Through querying we can find some information about the data you just wrote: