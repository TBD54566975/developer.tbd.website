## 7. Update DWN Records

Tp update the record, call `update` on the record object itself.

Add the following to `index.js`:
```js
const updateResult = await myRecord.record.update({data: "Hello, I'm updated!"});
```

<details>
<summary>Test your code</summary>
<p>
To see the record that was updated in the DWN, add the following to <code>index.js</code>:

```js
console.log('Update result: ', await myRecord.record.data.text());
```

The output will resemble:

```js
Update result: Hello, I'm updated!
```

</p>
</details>

Click "Run!" to see the updated record in the sandbox DWN: