import React from "react";
import { Admonition } from "@site/src/components/Admonition";

# Admonition Component

<div className="">
<p>The `Admonition` component is used to display a message with a specific style and icon.</p>

## Properties

| Property    | Type     | Required | Default | Description                       |
| ----------- | -------- | -------- | ------- | --------------------------------- |
| `variant`   | `string` | Yes      | `""`    | The variant of the admonition.    |
| `body`      | `string` | Yes      | `""`    | The content of the admonition.    |
| `className` | `string` | No       | `""`    | The class name of the admonition. |

## Usage

```jsx title="Admonition Component"
<Admonition variant="info" body="This is an info message." />
```

{/* variant Variant = "info" | "warning" | "tip" | "danger" | "note"; */}

## Examples

<p>Info</p>
<Admonition variant="info" body="This is an info message." />

<p>Warning</p>
<Admonition variant="warning" body="This is a warning message." />

<p>Tip</p>
<Admonition variant="tip" body="This is a tip message." />

<p>Danger</p>
<Admonition variant="danger" body="This is a danger message." />

<p>Note</p>
<Admonition variant="note" body="This is a note message." />

</div>
