---
hide_title: true
---

import newShnipTest from '!!raw-loader!@site/snippets/web5/build/verifiable-credentials/newShnipTest.snippet.js'

<LanguageSwitcher languages="JavaScript, Kotlin"/>


# Test Global Changes

Depending on the language you're programming in, you'll want to <LanguageSwitchLink text='link to different content' links='{"Kotlin": "https://kotlinlang.org/", "JavaScript": "https://www.javascript.com/"}'/>. Sometimes, you'll use the exact same link [for both languages](https://www.google.com/);

<Shnip
  snippets={[{ snippetContent: newShnipTest, language: 'JavaScript' }]}
  // 'inlineSnippets' is how we'll hardcode
  inlineSnippets={[
    {
      code: `
            val signedVcJwt = "eyJ0eXAiOiJKV1QiLCJhbGciOiJFZERTQSIsImtp..."
      `,
      language: 'Kotlin',
    },
  ]}
/>

# Adding a second one

<Shnip
  snippets={[{ snippetContent: newShnipTest, language: 'JavaScript' }]}
  // 'inlineSnippets' is how we'll hardcode
  inlineSnippets={[
    {
      code: `
data class EmploymentCredential(
  val position: String, 
  val startDate: String, 
  val employmentStatus: String )
            
val vc = VerifiableCredential.create(
    type = "EmploymentCredential",
    issuer = employer.didDocument.id.toString(),
    subject = employee.didDocument.id.toString(),
    data = EmploymentData(
        position = "Software Developer",
        startDate = "2023-04-01T12:34:56Z",
        employmentStatus = "Contractor"
    )
)
      `,
      language: 'Kotlin',
    },
  ]}
/>

