import React from 'react';

const SSIService = () => {
    return (
        <div>
            <div style={{ display: 'flex', alignItems: 'center' }}>
            </div>
            <h2><a>Prerequisites</a></h2>
            <p>Follow guide to <a href="run-ssi-service">Clone & Run SSI Service.</a></p>
        </div>
    );
};

export default SSIService;